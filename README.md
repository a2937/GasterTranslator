# Gaster Translator 

A Discord Bot that translates WingDings into English and back again.

To use: 
- Copy the `sample.env` to `.env`
- Fill out the information relevant to you
- Run `pnpm install`
- Run `pnpm run start`
- In Discord, find the `gasterify` and `degasterify` slash commands
- Success 
